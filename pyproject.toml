[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Lumache"
authors = [{name = "Nicola Coyle", email = "nicola.coyle@cefas.co.uk"}]
dynamic = ["version", "description"]
